package com.qaprosoft.gui.pages.common;

import org.openqa.selenium.WebDriver;

public abstract class SearchPageBase extends BasicPageBase {
    public SearchPageBase(WebDriver driver) {
        super(driver);
    }
}
